# Existing integration tests use the historical static admin token. This switch exists only
# on the test classpath; production and normal application configuration default to false.
app.admin-legacy-token-enabled=true
