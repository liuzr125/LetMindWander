const { profileService,friendService }=require('../../../services/index');
Page({data:{id:'',profile:{fields:{}},shared:[],loading:true,defaultAvatar:'/assets/logo.png',gender:['未设置','男','女','其他']},onLoad(o){this.setData({id:o.id||''});Promise.all([profileService.visible(o.id),friendService.shared(o.id).catch(()=>[])]).then(([profile,shared])=>this.setData({profile,shared,loading:false})).catch(e=>{this.setData({loading:false});wx.showToast({title:e.message||'资料加载失败',icon:'none'});});},shared(){wx.navigateTo({url:`/pages/friends/shared/index?id=${encodeURIComponent(this.data.id)}&name=${encodeURIComponent(this.data.profile.nickname||'')}`});},remove(){wx.showModal({title:'解除好友关系？',content:'解除后双方好友列表移除，已有知识授权立即失效。',confirmColor:'#e64242',success:r=>{if(r.confirm)friendService.remove(this.data.id).then(()=>wx.navigateBack({delta:1})).catch(e=>wx.showToast({title:e.message||'操作失败',icon:'none'}));}});}});
